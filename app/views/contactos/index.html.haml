.col-md-
%h2 Contactos

.row
    .col-md-12
        = link_to new_contacto_path, class: 'btn btn-primary btn-xs' do
            %span.glyphicon.glyphicon-plus
            Crear Nuevo Contacto

%br
        
.row
    .col-md-12
        
        %table.table.table-striped.table-hover
            %thead
                %th ID
                %th Nombre
                %th E-Mail
                %th Movil
                %th
                
            %tbody
                - @contactos.each do |contacto|
                    %tr
                        %td= contacto.id
                        %td= contacto.nombre_completo
                        %td= contacto.email
                        %td= contacto.movil
                        %td
                            = link_to [:edit, contacto], class: 'btn btn-default btn-xs' do
                                %span.glyphicon.glyphicon-pencil
                            = link_to contacto_path(contacto), method: :delete, class: 'btn btn-danger btn-xs' do
                                %span.glyphicon.glyphicon-trash
                            
                        
                        
                        